Todo
  Styles
    *Create base CSS config -- css-reset, fonts, colors, containers
      -global classes for common styles & CSS modules for components
    *use fontsource to install/import fonts 
      -create separate folder for fonts and import into index.js

  Components
    *determine component granularity and folder structure 
      -smalller utility components -- cards, buttons, inputs etc.
      -or larger single use components  
    
    
